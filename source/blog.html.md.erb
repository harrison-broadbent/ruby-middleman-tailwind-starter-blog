---
title: Blog
---

# Welcome to the blog!

This template includes the `middleman-blog` gem for blogging with markdown files.

This is the index page, located at `source/blog.html.md.erb`. The blog extension is configured to look inside `source/blog` for individual articles (this is configured inside `config.rb`).

Here are two example articles —

<ul>
  <% blog.articles.each do |article| %>
    <li class="space-x-4">
        <time>[<%= article.date.strftime('%b %e %Y') %>]</time>
        <%= link_to article.title, article.url %>
    </li>
  <% end %>
</ul>
